/**
 * HIIT Timer Component
 * Uses the new modern design with setup and active views
 */
import HiitTimerNew from './hiit/HiitTimerNew'

function HiitTimer(props) {
  return <HiitTimerNew {...props} />
}

export default HiitTimer
