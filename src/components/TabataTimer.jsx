/**
 * Tabata Timer Component
 * Uses the new modern design with setup and active views
 */
import TabataTimerNew from './tabata/TabataTimerNew'

function TabataTimer(props) {
  return <TabataTimerNew {...props} />
}

export default TabataTimer
